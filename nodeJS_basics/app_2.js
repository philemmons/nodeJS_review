
const log = require('./logger_1.js'); 
//logger.js is the related target we want to look
// same folder

//defined as a constant const

//console.log(logger);
//return object, single method a function
// { log: [Function: log] }

//logger.log('message');

// we define module
// we export one or more members
// to load the module we use the require function

// JSHint Demo

log('message');
//no longer a function, it's a functio we can call directly
//change logger to log
//F2 to rename